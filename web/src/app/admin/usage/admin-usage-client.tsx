/**
 * Deprecated split — admin gating now happens in /admin/layout.tsx, so
 * the dashboard lives back in ./page.tsx. This file is kept as a stub
 * to avoid build noise from any straggling import; safe to delete.
 */
export {};
